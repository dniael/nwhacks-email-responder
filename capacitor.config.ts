import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'nwhacks-email-responder',
  webDir: 'dist'
};

export default config;
